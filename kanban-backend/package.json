{
  "name": "kanban-backend",
  "version": "0.0.1",
  "description": "todo app with kanban board",
  "main": "index.js",
  "scripts": {
    "start": "node build/index.js",
    "tsc": "rm -rf build && tsc",
    "build": "rm -rf build && tsc && cd ../kanban-frontend && npm install && npm run build && mv build frontend && cp -r frontend ../kanban-backend/build && rm -rf frontend",
    "dev": "ts-node-dev index.ts",
    "lint": "eslint --ext .ts .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "juboskar",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@typescript-eslint/eslint-plugin": "^5.59.9",
    "@typescript-eslint/parser": "^5.59.9",
    "eslint": "^8.42.0",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "dotenv": "^16.1.4",
    "express": "^4.18.2",
    "pg": "^8.11.0",
    "sequelize": "^6.32.0"
  }
}
